#include "LinkedList.h"
#include<vector>
using namespace std;



class test{
    string name;

};
int main(){
    vector<int>a;
    a.push_back(1);
    a.push_back(2);
    a.push_back(3);
    int * p=&a[2];
    cout<<a[2]<<endl;
    a.pop_back();
    cout<<a[2]<<endl;
    

    // LinkedList<int>l;
    // l.append(1);
    // l.length();
    // l.print();
    // l.append(2);
    // l.length();
    // l.print();
    // l.append(3);
    // l.length();
    // l.print();
    // l.append(4);
    // l.length();
    // l.print();
    // l.append(5);
    // l.length();
    // l.print();
    // l.append(6);
    // l.length();
    // l.print();
    // l.append(7);
    // l.length();
    // l.print();
    // l.append(8);
    // l.length();
    // l.print();
    // l.append(9);
    // l.length();
    // l.print();
    

    // l.reverse();
    // l.print();

    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();

    
}